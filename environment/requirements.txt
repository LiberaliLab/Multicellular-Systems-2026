# Multicellular Systems 2026 — main course environment (Euler, Python 3.11.6)
#
#   module load stack/2024-05 gcc/13.2.0 python/3.11.6_cuda eth_proxy
#   python -m venv $HOME/venvs/mcs2026
#   source $HOME/venvs/mcs2026/bin/activate
#   pip install -r environment/requirements.txt
#
# Covers Part 1 (OME-Zarr) and Part 3 (analysis). Napari is a separate,
# laptop-only environment -- see requirements-napari.txt.

# --- OME-Zarr ---------------------------------------------------------------
# ngio pins the heavy stack for us: zarr>=3.1.6, numpy>=2.0, dask[array],
# anndata>=0.12.5,<0.14, pandas, polars, pyarrow, scipy, ome-zarr-models.
ngio==1.1.0
# ez-zarr only requires zarr>=3.0.0a0, so it coexists with ngio 1.1.0.
ez-zarr==0.4.2

# --- analysis ---------------------------------------------------------------
scanpy>=1.11
scikit-learn>=1.5
umap-learn>=0.5.6
leidenalg>=0.10
igraph>=0.11
# ForceAtlas2 layout for sc.tl.draw_graph (chapter 09). Without it scanpy does not
# fail -- it warns and silently falls back to igraph's 'fr', writing the result to
# obsm["X_draw_graph_fr"] instead of ["X_draw_graph_fa"].
fa2-modified>=0.4
# Geometric sketching -- Stage 1 chapter 04 reduces 650k cells to ~30k while
# keeping the rare states. Pure Python; its only extra dependency is fbpca.
geosketch>=1.3

# --- tables and I/O ---------------------------------------------------------
h5py>=3.11
openpyxl>=3.1          # reads the 384-well layout workbook
pyarrow>=16            # an ngio dependency; nothing in the course writes parquet
tqdm>=4.66

# --- plotting ---------------------------------------------------------------
matplotlib>=3.9
seaborn>=0.13

# --- notebook kernel --------------------------------------------------------
ipykernel>=6.29
